import { ObjectPageConfig } from '../../../../v2';
import { ExtensionLogger } from '../../../../apiTypes';
import { FileData } from '../../../common';
/**
 * External API: Create configuration (file content) for a V2 object page
 * @param {object} manifest  - manifest of the app
 * @param {string[]} flex - list of flex changes (from UI adaptatin)
 * @param {object} jsonSchema - app specific JSON schema
 * @param {string} entitySet - current entity set, identifying the given page
 * @param {FileData[]} fragments - list of all extension fragment files and their content
 * @param {ExtensionLogger} logger - Logger class for logging messages
 * @returns config - the object page configuration
 */
export declare function createObjectPageConfig(manifest: object, flex: string[], jsonSchema: object, entitySet: string, fragments?: FileData[], logger?: ExtensionLogger): ObjectPageConfig;
